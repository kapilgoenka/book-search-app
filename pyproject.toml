[project]
name = "book-search-app"
version = "0.1.0"
description = "Django-based book search web application with advanced filtering"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2.8",
    "gunicorn>=23.0.0",
]
